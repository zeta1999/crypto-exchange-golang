# Local code-intelligence index (machine-local, not shared)
.codegraph/

# Build artifacts
/bin/
/dist/
/exchange


# Runtime data (WAL, recorded feeds)
/data/
*.wal

# Feed recordings / fixtures generated locally
*.jsonl.tmp

# OS / editor cruft
.DS_Store
*.swp
/xxx.txt

# CCXT conformance harness binary
/conformance/ccxt-go/ccxt-go

# custody keystores (encrypted, but never commit) + binary
*.keystore.json
/cmd/custody/custody
